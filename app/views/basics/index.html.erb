<!DOCTYPE html>
<html>
    <head>
        <%= stylesheet_link_tag 'movies_button' %>
    </head>
    <body>
        <%#= javascript_include_tag show_tables' %>
        <table class="table table-dark">
            <tr>
                <th>Title</th>
                <th>Year</th>
                <th>Genre</th>
            </tr>
            <% @basics.each do |basic| %>
                <tr>
                    <td><%= basic.primaryTitle %></td>
                    <td><%= basic.startYear %></td>
                    <td><%= basic.genres %></td>
                </tdr>
            <% end %>
            <%= will_paginate @basics, class: 'apple_pagination' %>
        </table>

    </body>
</html>
