<%= form_with(model: rating) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
        <% rating.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tconst %>
    <%= form.text_field :tconst %>
  </div>

  <div class="field">
    <%= form.label :averageRating %>
    <%= form.text_field :averageRating %>
  </div>

  <div class="field">
    <%= form.label :numVotes %>
    <%= form.text_field :numVotes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
